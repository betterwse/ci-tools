{
  "name": "bw-ci-tools",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "prepare": "rm -rf build/templates && cp -r templates build && tsc",
  "build": "rm -rf build/templates && cp -r templates build && tsc"
  },
  "bin": {
    "bw-ci-tools": "build/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dot": "^1.1.3",
    "fast-xml-parser": "^4.4.0",
    "typescript": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
